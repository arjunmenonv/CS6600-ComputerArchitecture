class dispatchBuffer:
    def __init__(self):
        print("Created")

    def putIn(self): # play russian anthem xD (ggwp)
        return
    def dispatch(self):
        '''
            Initiate reg renaming, source reading and allocating RS, ROBuffer entries
            Stall if RRF, RS or ROBuffer is full

            Assumption: Dispatch buffer can dispatch up to M (= dispatch width) instructions,
            where dispatch width as defined in the config file
        '''
        return
